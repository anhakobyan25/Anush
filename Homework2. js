1. Given a number print its digits count. Use both loops (for, while)

let n = +prompt("Giv me number");
n = Math.abs(n)
let nString = n + "";
let  number= 0;
for(let i = 0; i < nString.length; i++){
number  = +nString.length
}
console.log(number)

1.1
let n = +prompt("Give me number");
n = Math.abs(n)
let nString = n + "";
let  number= 0;
let i = 0; 
while(i < nString.length) {
i++
number  = +nString.length
}
console.log(number)


2.Count numbers of digit 9 in a number. Use both loops (for, while)
let result = 0;
let num = prompt("Give me number");
while(num){
if(num % 10 === 9){
result++
}
num = Math.floor(num /10)
}
console.log(result)

2.1

let result = 0;
for ( let num = prompt("Give me number"); num = Math.floor(num /10); ) {
if( num % 10 === 9){
result++
}
}
console.log(result)



3. Given two numbers. Print ony odd numbers between that numbers.

for(let i = 7; i < 45; i++ ){
if(i % 2 !== 0)
alert(i)
}


3.1
let i = 0
while(i < 150 ){
i++
if(i % 2 !== 0)
alert(i)
}


4. Given a number as input, insert dashes (-) between each two even numbers.

let num = prompt("Give me number");
let last = undefined;
let str = "";
while(num){
	last = num % 10;
num = (num - last) /10;
		if(last % 2 === 0 && num %2 === 0){
		str = "-" + last + str
		}	else {
		str = last + str
		} 
}
console.log(str)






5. Given a positive number. Print it in reverse order.
let result = 0;
let num = prompt("")
  while (num) {
    result = result * 10 + num % 10;
    num = Math.floor(num / 10);
  }
console.log(result) 
